{
  "name": "@sourcerer/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:css": "tailwindcss -i ./src/styles.css -o ./src/tailwind.css",
    "dev": "npm run build:css && vite --host 0.0.0.0",
    "prebuild": "npm run build:css -- --minify",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.1",
    "tailwindcss": "^3.4.17",
    "vite": "^6.0.0"
  }
}
